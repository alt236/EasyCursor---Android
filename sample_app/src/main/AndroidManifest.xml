<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="dev.alt236.easycursor.sampleapp.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="dev.alt236.easycursor.sampleapp.activities.EasySqlCursorExampleActivity" />
        <activity android:name="dev.alt236.easycursor.sampleapp.activities.EasyJsonCursorExampleActivity" />
        <activity android:name="dev.alt236.easycursor.sampleapp.activities.EasyObjectCursorExampleActivity" />
    </application>

</manifest>